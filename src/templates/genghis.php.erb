<?php

/**
 * Genghis v<%= GENGHIS_VERSION %>
 *
 * The single-file MongoDB admin app
 *
 * http://genghisapp.com
 *
 * @author Justin Hileman <justin@justinhileman.info>
 */
define('GENGHIS_VERSION', <%= GENGHIS_VERSION.inspect %>);

<% includes.each do |inc| %><%= inc.sub(/(^<\?php\n|\?>\n?$)/, '').strip %>
<% end %>

$app = new Genghis_App(new Genghis_AssetLoader_Inline(__FILE__, __COMPILER_HALT_OFFSET__));
$app->run();

__halt_compiler();
<% assets.each do |asset| %>
@@<%= asset[:name] %> <%= asset[:etag] %>
<%= asset[:content] %>
<% end %>
