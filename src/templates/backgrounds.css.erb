html, body {
    background-image: url('<%= body %>');
}

.navbar-search .grippie {
    background-image: url('<%= grippie %>');
}

.nav .dropdown, .navbar-search {
    background-image: url('<%= nav %>');
}

section#servers tr.spinning td:first-child {
    background-image: url('<%= servers_spin %>');
}

body > section section.spinning > header h2 {
    background-image: url('<%= section_spin %>');
}
